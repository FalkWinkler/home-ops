---
# yaml-language-server: $schema=https://homelab-schemas-epg.pages.dev/external-secrets.io/externalsecret_v1.json
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: externaldns-unnbound
spec:
  refreshInterval: 5m
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword
  target:
    name: unbound-certificates
    creationPolicy: Owner
    template:
      type: kubernetes.io/tls
      engineVersion: v2
      data:
        ca.pem: '{{ index . "ca.crt" | b64enc }}'
        client.pem: '{{ index . "tls.crt" | b64enc }}'
        client.key: '{{ index . "tls.key" | b64enc }}'
  dataFrom:
    - extract:
        key: Unbound
