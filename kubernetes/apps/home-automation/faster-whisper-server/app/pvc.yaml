---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: "faster-whisper-server-data"
spec:
  accessModes:
    - ReadWriteOnce
  # dataSourceRef:
  #   kind: ReplicationDestination
  #   apiGroup: volsync.backube
  #   name: "whisper-bootstrap"
  resources:
    requests:
      storage: 2Gi
  storageClassName: ceph-block
